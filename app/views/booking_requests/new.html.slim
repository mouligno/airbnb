.container
  .row
    .col-md-12
      h1 New booking request

      = simple_form_for [@flat, @booking_request] do |form|
        = form.input :start_date
        = form.input :end_date
        = form.input :description
        = form.submit class: 'btn btn-primary'
